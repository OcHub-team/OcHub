---
title: 管理连接
description: 管理连接生命周期，并理解各应用的专属行为。
audience: human
sidebar:
  order: 1
---

OcHub 为每个应用分别保存连接。Claude Code 中的连接不会自动出现在 Codex 中，但可以
使用“复制到应用”转换为目标工具的格式。

## 自动发现已有配置

打开应用页面时，OcHub 会尝试读取该工具当前的实时配置：

- 能识别的连接会出现在“直接连接”列表中。
- 已写入工具但尚未设为当前项的连接会显示“已添加”。
- 当前正在使用的连接会显示“当前”。
- 无法完整识别的自定义字段应先查看写入预览，避免重新保存时丢失。

## 连接的日常操作

| 操作 | 适用场景 |
| --- | --- |
| 编辑 | 修改地址、密钥、模型、备注或高级字段 |
| 拖动排序 | 调整列表和系统菜单中的显示顺序 |
| 复制到应用 | 用目标应用的格式另存一份，不改动原连接 |
| 添加到工具 | 预先写入工具，但保持当前连接不变 |
| 切换 | 立即把它设为当前连接 |
| 从工具移除 | 保留 OcHub 数据，只移除工具侧配置 |
| 删除 | 从 OcHub 永久删除连接 |

删除前如果该连接仍是当前项，先切换到其他项。

## 官方登录

“使用官方登录”不会写入第三方 Base URL 或 API Key，而是沿用工具自身的登录状态。适合：

- Claude Code 或 Codex 已完成官方账号登录。
- 临时从第三方 API 切回官方服务。
- 希望保留工具原生的账号和订阅能力。

Codex 的“第三方连接保留当前 OAuth”开启后，切换第三方连接时不会覆盖 `auth.json`。
这是应用专属设置，不影响其他工具。

## 应用专属设置

在应用页面右上角打开“应用设置”：

| 应用 | 主要设置 |
| --- | --- |
| Claude Code | 跳过 MCP 引导、Claude 插件集成、自定义配置目录 |
| Codex | 保留 OAuth、统一会话历史、迁入既有会话、自定义配置目录 |
| OpenCode | 自定义配置目录 |
| OpenClaw | 自定义配置目录 |
| Hermes | 自定义配置目录 |
| Grok Build | 自定义配置目录 |

自定义目录会影响配置、MCP 或会话历史的读取位置。保存后建议重启 OcHub 和目标工具。

## 编辑器中的检查工具

- **测试 URL**：测量端点返回 HTTP 响应的延迟。
- **拉取模型**：使用当前地址和密钥获取模型列表。
- **查询余额**：仅对实现了余额接口的供应商有效。
- **将写入的文件**：展示保存或切换时生成的配置内容和目标路径。
- **通用配置**：把应用级共享片段合并到启用该选项的连接。

测试成功不代表模型一定可调用。最终以目标 CLI 发起的真实请求为准。

## 配置被外部修改

OcHub 会在写入前检查实时配置是否发生漂移。出现冲突时：

1. 查看“将保留”“将保持删除”和“无法同时保留”三组差异。
2. 能确认外部字段仍需要时，选择“保留我的修改”。
3. 希望完全采用 OcHub 预览时，选择“丢弃我的修改”。
4. 无法判断时取消，先备份文件并比较。

不要让 OcHub、cc-switch 和手工编辑器同时修改同一份配置。

## 按应用查看字段说明

| 应用 | 继续阅读 |
| --- | --- |
| Claude Code / Claude Desktop | [鉴权变量、角色模型、MCP 与插件](/zh/guides/claude) |
| Codex | [OAuth、Responses、推理与会话历史](/zh/guides/codex) |
| Grok Build / OpenCode / OpenClaw / Hermes | [协议、模型列表与专属配置](/zh/guides/open-tools) |

需要从旧管理器带入连接时，不要手工逐条复制，先查看
[从 cc-switch 迁移](/zh/guides/migration)。
