[toolchain]
# Zed pins 1.95.0 for its own builds; anything >= that works for the GPUI
# path dependency, so we track the current stable.
channel = "1.97.1"
profile = "minimal"
components = ["rustfmt", "clippy"]
