# OcHub application icons

These files are derived from the green OcHub artwork supplied by the project
owner. `ochub-1024.png` is the raster master used to generate the smaller PNG
sizes, `ochub.icns`, and `ochub.ico`.

Keep the filenames stable: the desktop build, QA bundle, and release packager
reference them directly.
